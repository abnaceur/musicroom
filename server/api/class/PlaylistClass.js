const mongoose = require('mongoose');
const utils = require('../utils/utils');

async function CreateNewPlaylist(data, token) {
    return new Promise(async (resolve, reject) => {

    })

}


module.exports = {
    CreateNewPlaylist
}