const playListDao = require('../../daos/playListDao/playListDao');
const certificateDao = require('../../daos/certificateDao/certificateDao');

async function updatePlaylist(res, data, user) {
}

async function deletePlaylist(res, data, user) {
}


module.exports = {
    updatePlaylist,
    deletePlaylist
}